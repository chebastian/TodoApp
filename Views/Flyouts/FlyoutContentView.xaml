<UserControl x:Class="Views.Flyouts.FlyoutContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Views.Flyouts"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <SolidColorBrush x:Key="PopupBkg" Color="{StaticResource Color_ButtonHoverBackground}"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Border Background="{StaticResource PopupBkg}" Margin="0 0 0 0" CornerRadius="5" Grid.Column="1" >
            <ContentPresenter Margin="10" Content="{Binding}"/>
        </Border>
        <Polygon Margin="0 0 -4 0"  Points="0,0 8,0 8,20" Fill="{StaticResource PopupBkg}"/>
        <Grid.Effect>
            <DropShadowEffect BlurRadius="3" ShadowDepth="2" />
        </Grid.Effect>
    </Grid>
</UserControl>
